# @waysnx/ui-files — AI Agent Guide

> **Part of the WaysNX UI Kit.** Full integration guide: see `@waysnx/ui-kit` LLM.md (shipped with that package).

---

## ⭐ What this package does

File and document viewing — PDF viewer with pagination/zoom and smart document preview with automatic type detection.

---

## Package info

- **npm:** `@waysnx/ui-files` v1.0.0 (companion package — NOT in `@waysnx/ui-kit` meta)
- **Install:** `npm install @waysnx/ui-files`
- **Peer deps:** `react >=18`, `react-dom >=18`
- **CSS (required):** `import '@waysnx/ui-files/dist/index.css'`

---

## Exported components

| Component | Purpose |
|-----------|---------|
| `PDFViewer` | PDF document viewer with pagination and zoom |
| `DocumentPreview` | Smart document preview with automatic type detection |
